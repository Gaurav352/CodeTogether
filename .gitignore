# Node modules
node_modules/
**/node_modules/

# Environment / secrets
.env
.env.*        # .env.local, .env.production, etc.
.env.local
.env.production.local
.env.test

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
/pm2.log
/pm2.pid

# Build outputs (frontend/backend)
dist/
build/
out/
public/build/
.vite/
.cache/
.cache-loader/
.next/          # Next.js builds (if ever used)
.astro/
.svelte-kit/

# Vite specific
/.vite

# Parcel / Rollup caches
.parcel-cache/

# Coverage
coverage/
coverage-final.json
.nyc_output/

# Test results
test-results/
jest-cache/
playwright-report/

# Local runtime files
pids/
*.pid
*.seed
*.pid.lock
*.sock

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# IDE/editor
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*~.nib
*.code-workspace

# MacOS Finder attributes
.AppleDouble
.LSOverride

# SASS cache
.sass-cache

# TypeScript
*.tsbuildinfo

# Build artifacts for webpack etc.
dist-ssr/
stats.json

# Static/uploads (do NOT commit user uploaded files)
uploads/
public/uploads/
tmp/
temp/

# Database / local storage
*.sqlite
*.db
*.dump
mongod.lock
dump/
mongo-data/

# Docker
docker-compose.override.yml
.dockerignore
docker-compose.*.yml
docker-compose.override.yml

# Containers
node-app-*.log

# Secrets / certs
*.pem
*.key
*.crt
secrets/
certs/
private/

# Package managers locks (optional: keep one lock if you prefer)
# If you commit lock files, remove the following lines for that lock type:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Lerna / Turborepo / pnpm workspaces
.pnpm-store/
.turbo/
.cache/

# Misc
.envrc
.idea_modules/
*.iml
.vagrant/
coverage/
.snyk

# macOS Spotlight (just in case)
.Spotlight-V100

# Ignore build artifacts produced by VS Code extensions
.history/

# Optional: remove if you need them in repo
# local config files
.env.*.local
